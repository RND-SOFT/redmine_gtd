/ = content_tag :fieldset, class: 'box' do 
%p
  %label= t('.default_status_work') 
  = select_tag "settings[status_work_id]", options_from_collection_for_select(IssueStatus.all, :id, :name, selected: @settings[:status_work_id]) 

%p
  %label= t('.default_status_standby') 
  = select_tag "settings[status_standby_id]", options_from_collection_for_select(IssueStatus.all, :id, :name, selected: @settings[:status_standby_id]) 

/   %label= l(:label_checklist_save_log)
/   = check_box_tag 'settings[save_log]', 1, @settings[:save_log]
/ - if Setting.issue_done_ratio == "issue_field"
/   %p
/     %label= l(:label_checklist_done_ratio) 
/     = check_box_tag 'settings[issue_done_ratio]', 1, @settings[:issue_done_ratio]
